
import React from 'react';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export const FinishedGoodsFormTab = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Add/Edit Products</CardTitle>
      </CardHeader>
      <CardContent>
        <p className="text-muted-foreground">
          Product configuration form will be implemented here.
        </p>
      </CardContent>
    </Card>
  );
};
